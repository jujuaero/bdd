# Database connection settings (overrides defaults in code)
url=jdbc:mysql://localhost:3306/artconnect_db?useSSL=false&serverTimezone=UTC
user=root
password=Password123!
# Enable or disable persistence
usePersistence=true
# Administrator password used by the UI login (override this for production)
# Change the value below to a secure password before deployment
adminPassword=admin

